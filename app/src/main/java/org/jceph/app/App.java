/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package org.jceph.app;

import org.jceph.list.LinkedList;

import static org.jceph.utilities.StringUtils.join;
import static org.jceph.utilities.StringUtils.split;
import static org.jceph.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
